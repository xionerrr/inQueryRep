import { motion } from 'framer-motion';
import styled from "styled-components"

export const StyledNoMatchPage = styled(motion.div)`
    text-align: center;
    padding: 5vw;
    font-size: 48px;
    font-weight: 500;
`